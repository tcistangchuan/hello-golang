- 进程和线程、协程概念

  ```
  进程：是一个单独功能的程序，进程有自己独立的内存空间，不同进程间通过通信来通信，由于进程比较重量，所以上下文进程间的切换开销比较大，但是比较稳定
  
  线程：线程是进程的一个实体，是cpu调度和分配的最小单位。线程间通过共享内存来通信，上下文切换很快，资源开销较少，但是没有进程稳定，容易丢失资数据
  
  协程：协程是一种用户态的轻量级线程，内存占用很小，协程的调度有用户控制，协程调度切换很快。
  ```

  

